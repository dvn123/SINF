'use strict';

describe('myApp.viewProducts module', function() {

    beforeEach(module('myApp.viewProducts'));

    describe('viewProducts controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var viewProductsCtrl = $controller('viewProductsCtrl');
            expect(viewProductsCtrl).toBeDefined();
        }));

    });
});